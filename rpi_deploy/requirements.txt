# Python dependencies for RVM deployment on Raspberry Pi 5
# NOTE: No ultralytics/torch needed! We use OpenCV DNN for inference.
# Heavy packages (numpy, scipy, opencv, matplotlib) come from system apt.
flask>=3.0,<4.0
flask-socketio>=5.3,<6.0
psutil>=5.9,<7.0
